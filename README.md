# Generic_RMI_Client_Server

## Description
This system implements the client-server model with Java RMI. 
The objective of this project is generic message exchange between client-server through Object class

## Usage

